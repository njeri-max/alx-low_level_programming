#include "main.h"
/**
 * mul - functions that takes in two integers and multiplies them
 * @a: first integer input
 * @b: second  integer input
 * Return: multiplication results of the inputs
 */
int mul(int a, int b)
{
	return (a * b);
}
