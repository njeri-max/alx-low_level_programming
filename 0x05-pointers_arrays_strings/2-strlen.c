#include "main.h"
/**
 * _strlen - function that returns length of a string
 * @s: char type pointer
 * Return: length of string
 */
int _strlen(char *s)
{
	int z;

	for (z = 0; s[z] != '\0'; z++)
	{
		;
	}
	return (z);
}

